# Scenario

**Feature**: `tsk topic view` prints the topic / sub-topic / task tree

```
tsk topic view [--json] <topic>
```

```go
func Setup(t *testing.T, d *session.Doctest, req *Request) error {
	ensureHelpersUsed()
	return nil
}
```
