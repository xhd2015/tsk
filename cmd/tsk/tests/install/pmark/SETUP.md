# Scenario

**Feature**: `tsk install pmark` installs project-add forwarder

```
HOME=WorkRoot
tsk install pmark -> ~/.local/bin/pmark + PATH checker
```

## Preconditions

- Parent `install/SETUP.md` sets `HOME=WorkRoot`.
